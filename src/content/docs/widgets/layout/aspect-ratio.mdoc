---
title: AspectRatio
description: 根据父约束和目标宽高比确定布局尺寸。
---

`AspectRatio` 让宽度与高度尽可能满足指定比值，同时仍遵守父约束。

```kotlin
fun ChildSlot.AspectRatio(
    aspectRatio: Float,
    content: AspectRatio.() -> Unit = {},
)
```

`aspectRatio` 是宽度除以高度，必须是有限正数。父节点至少需要给出一个有界的最大宽度或高度；双轴无界时无法计算尺寸。若父约束与比例冲突，最终以父约束为准，子节点填满计算出的尺寸。

```kotlin
ConstrainedBox(constraints = BoxConstraints(maxWidth = 240f, maxHeight = 160f)) {
    AspectRatio(aspectRatio = 2f) {
        ColoredBox(color = 0xFF2196F3.toInt())
    }
}
```

Parser 对应 `<AspectRatio aspectRatio="2">…</AspectRatio>`，最多一个子节点。
